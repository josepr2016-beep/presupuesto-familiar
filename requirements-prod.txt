# requirements-prod.txt
# ----------------------
# Dependencias adicionales SOLO para cuando la app corre en un servidor
# en la nube (Render, Railway, etc.). No se instalan en tu computador local:
# tu PC usa SQLite y no necesita servidor de producción.
#
# Render instala esto automáticamente (ver render.yaml). Si configuras el
# despliegue manualmente, el Build Command debe ser:
#   pip install -r requirements.txt -r requirements-prod.txt

gunicorn==22.0.0
psycopg2-binary==2.9.12
