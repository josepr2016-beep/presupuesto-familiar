Flask==3.0.3
Flask-SQLAlchemy==3.1.1
python-dateutil==2.9.0.post0

# --- Necesarios solo para producción/nube (ej. Render, Railway) ---
# No son requeridos para ejecutar localmente con "python app.py" + SQLite,
# pero no hacen daño si se instalan también en local.
gunicorn==22.0.0
psycopg2-binary==2.9.9
